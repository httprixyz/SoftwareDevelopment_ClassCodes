void main() {
  int numero = 19; //coloque um número aqui
  
  bool ehPar = numero % 2 == 0;
  
  String mensagem = ehPar ? 'par' : 'ímpar';
  
  String sep = '-';
  
  DateTime agora = DateTime.now();
  
  print('${sep} ${numero} ${sep} é um número ${mensagem}'.toUpperCase());
  print('Data e hora atuais: $agora');
}
